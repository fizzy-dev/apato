const getLogin = async (req, res, next) => {
    try {

    } catch (e) {
        next(e);
    }
}

const logout = async (req, res, next) => {
    try {

    } catch (e) {
        next(e);
    }
}

module.exports = {
    getLogin,
    logout
}